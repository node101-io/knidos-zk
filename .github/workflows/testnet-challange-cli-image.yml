name: testnet-challange-cli image

on:
  push:
    branches: [main]
    paths:
      - 'packages/testnet-challange/**'
      # Noir circuit source — the image runs `nargo compile` on this at build
      # time and also bundles it for the user to recompile on their machine.
      - 'circuit/src/**'
      - 'circuit/Nargo.toml'
      # Patch applied to the noir_json_parser submodule during warmup.
      - 'patches/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/testnet-challange-cli-image.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/node101-io/knidos-challenge

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # noir_json_parser is a git submodule referenced from circuit/Nargo.toml
          # and the docker warmup stage runs `nargo compile` which fails without it.
          submodules: recursive

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          short_sha="${GITHUB_SHA::7}"
          echo "tags=${IMAGE}:latest,${IMAGE}:${short_sha}" >> "$GITHUB_OUTPUT"

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/testnet-challange/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
