[package]
name = "circuit"
type = "bin"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
sha256 = { git = "https://github.com/noir-lang/sha256", tag = "v0.3.0" }
json_parser = { path = "../noir_json_parser" }